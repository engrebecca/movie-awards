import React from 'react';
import { Button } from 'reactstrap';

const Btn = (props) => {
    return (
        <div>
            <Button color="secondary">secondary</Button>{' '}
        </div>
    );
}

export default Btn;